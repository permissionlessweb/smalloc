[package]
name = "smmalloc-ffi"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
smmalloc = { path = "../smmalloc", package = "smmalloc" }

[features]
growers = []
